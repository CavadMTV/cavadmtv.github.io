name: M3U Playlist Güncelleyici

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */6 * * *'

jobs:
  update-m3u:
    runs-on: ubuntu-latest
    
    steps:
      - name: ⬇️ Depoyu Çıkar
        uses: actions/checkout@v4

      - name: 🐍 Python'ı Kur
        uses: actions/setup-python@v5
        with:
          python-version: '3.x' 

      - name: 📦 Gerekli Kütüphaneleri Kur
        run: pip install requests

      - name: ⚙️ M3U Dosyasını İndir ve Güncelle
        run: python files/m3u_ch.py
        
      - name: 🔄 Değişiklikleri Depoya Kaydet (Commit)
        run: |
          if [ -n "$(git status --porcelain)" ]; then
            git config user.name "GitHub Actions Bot"
            git config user.email "actions@github.com"
            git add playlist.m3u
            git commit -m "chore: Otomatik M3U playlist güncellemesi"
            git push
          else
            echo "Dosyada değişiklik yok, commit atlanıyor."
          fi
          
